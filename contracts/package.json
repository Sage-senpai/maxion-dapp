{
  "name": "maxion-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for MAXION on Mantle",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:testnet": "hardhat run scripts/deploy.js --network mantleTestnet",
    "deploy:mainnet": "hardhat run scripts/deploy.js --network mantleMainnet"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "@openzeppelin/contracts": "^5.0.0",
    "dotenv": "^16.3.1",
    "hardhat": "^2.19.0"
  }
}