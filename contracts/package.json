{
  "name": "maxion-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for MAXION dApp on Mantle",
  "type": "module",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:testnet": "hardhat run scripts/deploy.ts --network mantleTestnet",
    "deploy:mainnet": "hardhat run scripts/deploy.ts --network mantleMainnet",
    "node": "hardhat node"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@openzeppelin/contracts": "^5.0.1",
    "@types/node": "^25.0.2",
    "dotenv": "^17.2.3",
    "hardhat": "^2.28.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}
